# 美术资源工具集 (Art Asset Tools)

**作者**: Manus AI  
**版本**: 1.0.0  
**日期**: 2025-12-08

---

本目录包含一系列用于辅助《锈湖：炼金术士的天体仪》项目美术资源管理的命令行工具。

## 1. 动画序列验证工具 (Animation Sequence Validator)

**脚本**: `validate_animation_sequences.py`

**用途**: 自动扫描 `Content/Art` 目录下的所有美术资源，检查动画序列的完整性和连续性，并生成详细的验证报告。

### 功能特性

- **自动识别**: 能够自动识别以 `_` 加数字（如 `_01`, `_1`）结尾的PNG文件，并将其归类为动画序列。
- **完整性检查**: 验证一个序列的帧数是否满足最小数量要求（默认为3帧）。
- **连续性检查**: 确保序列的帧序号是连续的（如 1, 2, 3, 4），没有跳帧或缺失。
- **多变体识别**: 能够智能识别并区分合理的多变体设计（如同一帧号但后缀不同，`_Blank`, `_Starry`），避免误报为错误。
- **报告生成**: 生成清晰的文本报告，汇总统计信息（完整、警告、错误），并列出每个序列的详细检查结果。

### 使用方法

在项目根目录下执行以下命令：

```bash
python3 Tools/validate_animation_sequences.py
```

脚本将自动扫描默认的资源目录 (`Content/Art`)，并在控制台打印报告。同时，一份详细的报告将保存至 `Docs/REPORTS/animation_validation_report.txt`。

你也可以指定一个不同的美术资源目录作为参数：

```bash
python3 Tools/validate_animation_sequences.py /path/to/your/art/directory
```

## 2. 资源预览图册生成工具 (Asset Catalog Generator)

**脚本**: `generate_asset_catalog.py`

**用途**: 自动扫描 `Content/Art` 目录下的所有美术资源，并生成一个独立的、可交互的HTML预览图册，方便团队成员快速浏览、搜索和评估所有资源。

### 功能特性

- **自动扫描与分类**: 自动遍历所有资源，并按“章节”和“类别”（如Icons, Props, Scenes）进行组织。
- **Base64内联**: 将所有图片资源通过Base64编码直接嵌入HTML文件中，生成一个完全独立的单文件图册，无需Web服务器即可本地打开。
- **统计信息**: 在页面顶部显示总资源数、章节数、类别数等关键统计数据。
- **交互式预览**: 
    - **网格布局**: 以卡片形式清晰展示每个资源。
    - **详细信息**: 每个卡片显示资源名称和文件大小。
    - **点击放大**: 点击任意图片可全屏模态框放大预览。
- **实时搜索**: 提供一个搜索框，可以根据资源名称动态过滤和查找特定资源。
- **美观的UI**: 采用暗色主题，设计风格与“锈湖”系列保持一致，提供良好的视觉体验。

### 使用方法

在项目根目录下执行以下命令：

```bash
python3 Tools/generate_asset_catalog.py
```

脚本将自动扫描默认的资源目录 (`Content/Art`)，并生成预览图册。完成后，最终的HTML文件将保存至 `Docs/CATALOG/asset_catalog.html`。

**注意**: 由于所有图片都被内联到HTML文件中，生成的文件可能会非常大（例如，对于139个资源，文件大小可能超过250MB）。请耐心等待脚本执行完毕。

生成后，直接在浏览器中打开该HTML文件即可查看。

```bash
# 例如，在macOS上
open Docs/CATALOG/asset_catalog.html

# 在Windows上
start Docs\CATALOG\asset_catalog.html

# 在Linux上
xdg-open Docs/CATALOG/asset_catalog.html
```


## 3. 透明度检查工具 (Transparency Checker)

**脚本**: `check_transparency.py`

**用途**: 批量检查 PNG 图片的透明度状态，识别假透明图片（RGB 模式或 RGBA 但完全不透明）。

### 功能特性

- **自动识别**: 根据文件名前缀判断哪些文件应该是透明底的（ITEM_、OBJ_、CHAR_、PLANT_、PAINT_）
- **透明度检测**: 检查图片的颜色模式（RGB/RGBA）和 alpha 通道的实际值
- **分类统计**: 将图片分为真透明、假透明、背景图片等类别
- **生成文件列表**: 输出需要转换的假透明图片列表

### 使用方法

在项目根目录下执行以下命令：

```bash
cd Content/Art
python3 ../../Tools/check_transparency.py .
```

脚本将扫描指定目录下的所有 PNG 文件，并生成 `/tmp/fake_transparent_list.txt` 文件列表。

### 输出示例

```
📊 统计结果:
  - RGB 模式（应该透明）: 121 个
  - RGBA 模式（假透明）: 0 个
  - RGBA 模式（真透明）: 0 个
  - 背景图片（不需要透明）: 37 个
```

---

## 4. 绿幕转换工具 (Greenscreen Converter)

**脚本**: `convert_to_greenscreen.py`

**用途**: 将假透明图片批量转换为绿幕背景，解决 NanoBanana Pro 无法生成真透明图片的问题。

### 功能特性

- **智能背景检测**: 自动识别灰白色棋盘格或浅色背景像素
- **精确替换**: 将背景像素替换为标准绿幕颜色 #00FF00
- **保持前景**: 只替换背景，保持物体本身的颜色和细节不变
- **批量处理**: 支持从文件列表批量转换
- **进度显示**: 显示每个文件的转换进度和背景占比

### 技术参数

- **绿幕颜色**: #00FF00 (R=0, G=255, B=0)
- **背景检测阈值**:
  - 浅色像素：RGB 值都 > 200
  - 灰色像素：RGB 值相近且 > 180（棋盘格）

### 使用方法

```bash
# 1. 先检查并生成文件列表
python3 Tools/check_transparency.py Content/Art

# 2. 批量转换
python3 Tools/convert_to_greenscreen.py /tmp/fake_transparent_list.txt
```

### 输出示例

```
准备转换 121 个文件...
================================================================================
  ✓ 转换完成: ITEM_Crown_Icon.png (39.6% 背景)
  ✓ 转换完成: ITEM_Magnifier_Icon.png (45.2% 背景)
  ...
================================================================================
📊 转换统计:
  ✓ 成功: 121 个
  ⊘ 跳过: 0 个
  ✗ 失败: 0 个
```

---

## 工作流程：处理新增美术资产

### 1. 生成资产
使用 NanoBanana Pro 生成美术资产（注意：生成的图片是假透明）

### 2. 检查透明度
```bash
python3 Tools/check_transparency.py Content/Art
```

### 3. 转换为绿幕
```bash
python3 Tools/convert_to_greenscreen.py /tmp/fake_transparent_list.txt
```

### 4. 验证结果
检查转换后的图片是否正确，确认背景是纯绿色

### 5. 提交到 Git
```bash
git add Content/Art
git commit -m "feat: 添加新美术资产（绿幕背景）"
```

---

## 为什么使用绿幕而不是真透明？

### 1. 兼容性
- 绿幕是游戏引擎的标准做法
- UE4 原生支持 Chroma Key 抠图

### 2. 可视化
- 绿色背景清晰可见，便于调试
- 真透明在某些编辑器中难以区分

### 3. 灵活性
- 可以调整抠图参数（容差、边缘羽化等）
- 真透明一旦生成就无法调整

---

## UE4 集成配置

在 Unreal Engine 中使用绿幕资产：

### 创建 Chroma Key Material
1. 使用 Material Editor
2. 添加 Chroma Key 节点

### 设置参数
- **Key Color**: (0, 1, 0) - 纯绿色
- **Tolerance**: 0.1 - 0.3（根据需要调整）
- **Edge Feather**: 0.01 - 0.05（边缘羽化）

### 应用到 Sprite
- 将 Material 应用到 Paper2D Sprite
- 或者应用到 UI Widget

---

## 依赖

所有脚本需要以下 Python 库：

```bash
pip3 install pillow numpy
```

---

## 版本历史

### v1.1.0 (2024-12-08)
- 新增 `check_transparency.py` - 透明度检查工具
- 新增 `convert_to_greenscreen.py` - 绿幕转换工具
- 批量转换了 121 个假透明图片为绿幕背景

### v1.0.0 (2024-12-07)
- 初始版本
- 包含动画序列验证工具和资源预览图册生成工具
